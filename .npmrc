# Security settings for npm
audit=true
fund=false
audit-level=high

# Prevent installation of packages with vulnerabilities
engine-strict=true

# Ensure package-lock.json is used
package-lock=true

# Disable running scripts from dependencies during installation
ignore-scripts=true
